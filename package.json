{
  "name": "scb-mcp",
  "version": "2.1.0",
  "description": "Model Context Protocol server for Statistics Sweden (SCB) and E-hälsomyndigheten (Swedish eHealth Agency) PX-Web APIs",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "start:http": "node dist/http-server.js",
    "start:mcp": "node dist/http-mcp-server.js",
    "dev": "tsc --watch",
    "test": "npm run test-api",
    "test-api": "npm run build && node test-api.mjs",
    "test-validation": "npm run build && node tests/api-validation.test.mjs",
    "test-local": "npm run build && node local-test.js",
    "test-simple": "npm run build && node simple-test.js",
    "test-full": "npm run build && node test-tools.js",
    "prepare": "npm run build"
  },
  "keywords": [
    "mcp",
    "statistics",
    "sweden",
    "scb",
    "px-api",
    "claude"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@types/express": "^5.0.5",
    "express": "^5.1.0",
    "node-fetch": "^3.3.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
